{{- if .Values.hyperzoektWebui.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "hyperzoekt.fullname" . }}-hyperzoekt-webui
  labels:
    {{- include "hyperzoekt.labels" . | nindent 4 }}
    app.kubernetes.io/component: hyperzoekt-webui
  {{- with .Values.hyperzoektWebui.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.hyperzoektWebui.replicaCount }}
  selector:
    matchLabels:
      {{- include "hyperzoekt.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: hyperzoekt-webui
  template:
    metadata:
      labels:
        {{- include "hyperzoekt.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: hyperzoekt-webui
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "hyperzoekt.serviceAccountName" . }}
      containers:
        - name: hyperzoekt-webui
          image: {{ .Values.hyperzoektWebui.image.registry }}/{{ .Values.hyperzoektWebui.image.repository }}:{{ .Values.hyperzoektWebui.image.tag }}
          imagePullPolicy: {{ .Values.hyperzoektWebui.image.pullPolicy }}
          command: ["/app/hyperzoekt-webui", "--host", "0.0.0.0", "--port", {{ .Values.hyperzoektWebui.service.port | quote }}]
          ports:
            - name: http
              containerPort: {{ .Values.hyperzoektWebui.service.port }}
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          env:
            - name: REDIS_URL
              value: {{ include "hyperzoekt.redisUrl" . | quote }}
            - name: SURREALDB_URL
              value: {{ include "hyperzoekt.surrealUrl" . | quote }}
            - name: SURREALDB_USERNAME
              value: {{ .Values.surrealdb.external.username | default "root" | quote }}
            - name: SURREALDB_PASSWORD
              value: {{ .Values.surrealdb.external.password | default "root" | quote }}
            - name: SURREAL_NS
              value: {{ .Values.surrealdb.external.namespace | default "zoekt" | quote }}
            - name: SURREAL_DB
              value: {{ .Values.surrealdb.external.database | default "repos" | quote }}
            - name: HZ_LLM_URL
              value: {{ .Values.defaultLlmUrl | quote }}
            {{- with .Values.hyperzoektWebui.env }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          resources:
            {{- toYaml .Values.hyperzoektWebui.resources | nindent 12 }}
      {{- with .Values.hyperzoektWebui.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.hyperzoektWebui.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.hyperzoektWebui.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
---
{{- if and .Values.hyperzoektWebui.enabled (eq .Values.hyperzoektWebui.exposure.type "service") }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "hyperzoekt.fullname" . }}-hyperzoekt-webui
  labels:
    {{- include "hyperzoekt.labels" . | nindent 4 }}
    app.kubernetes.io/component: hyperzoekt-webui
spec:
  type: {{ .Values.hyperzoektWebui.exposure.serviceType }}
  ports:
    - port: {{ .Values.hyperzoektWebui.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hyperzoekt.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: hyperzoekt-webui
{{- end }}
---
{{- if and .Values.hyperzoektWebui.enabled (eq .Values.hyperzoektWebui.exposure.type "ingress") .Values.hyperzoektWebui.exposure.ingress.enabled }}
{{- $fullName := include "hyperzoekt.fullname" . }}
{{- $svcPort := .Values.hyperzoektWebui.service.port }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}-hyperzoekt-webui
  labels:
    {{- include "hyperzoekt.labels" . | nindent 4 }}
    app.kubernetes.io/component: hyperzoekt-webui
  {{- with .Values.hyperzoektWebui.exposure.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.hyperzoektWebui.exposure.ingress.className }}
  ingressClassName: {{ .Values.hyperzoektWebui.exposure.ingress.className }}
  {{- end }}
  {{- if and .Values.hyperzoektWebui.exposure.ingress.tls.enabled .Values.hyperzoektWebui.exposure.ingress.tls.secretName }}
  tls:
    - hosts:
        - {{ .Values.hyperzoektWebui.exposure.ingress.host | quote }}
      secretName: {{ .Values.hyperzoektWebui.exposure.ingress.tls.secretName }}
  {{- end }}
  rules:
    - host: {{ .Values.hyperzoektWebui.exposure.ingress.host | quote }}
      http:
        paths:
          - path: {{ .Values.hyperzoektWebui.exposure.ingress.path }}
            {{- if and .Values.hyperzoektWebui.exposure.ingress.pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .Values.hyperzoektWebui.exposure.ingress.pathType }}
            {{- end }}
            backend:
              service:
                name: {{ $fullName }}-hyperzoekt-webui
                port:
                  number: {{ $svcPort }}
{{- end }}
