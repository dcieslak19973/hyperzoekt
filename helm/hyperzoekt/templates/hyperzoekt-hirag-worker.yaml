{{- if and (hasKey .Values "hyperzoektHiragWorker") .Values.hyperzoektHiragWorker.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "hyperzoekt.fullname" . }}-hyperzoekt-hirag-worker
  labels:
    {{- include "hyperzoekt.labels" . | nindent 4 }}
    app.kubernetes.io/component: hyperzoekt-hirag-worker
  {{- with .Values.hyperzoektHiragWorker.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.hyperzoektHiragWorker.replicaCount }}
  selector:
    matchLabels:
      {{- include "hyperzoekt.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: hyperzoekt-hirag-worker
  template:
    metadata:
      labels:
        {{- include "hyperzoekt.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: hyperzoekt-hirag-worker
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "hyperzoekt.serviceAccountName" . }}
      containers:
        - name: hyperzoekt-hirag-worker
          image: {{ .Values.hyperzoektHiragWorker.image.registry }}/{{ .Values.hyperzoektHiragWorker.image.repository }}:{{ .Values.hyperzoektHiragWorker.image.tag }}
          imagePullPolicy: {{ .Values.hyperzoektHiragWorker.image.pullPolicy }}
          command: ["/app/hyperzoekt-hirag-worker", "--host", "0.0.0.0", "--port", "{{ .Values.hyperzoektHiragWorker.service.port }}"]
          ports:
            - name: http
              containerPort: {{ .Values.hyperzoektHiragWorker.service.port }}
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          env:
            - name: REDIS_URL
              value: {{ include "hyperzoekt.redisUrl" . | quote }}
            - name: SURREALDB_URL
              value: {{ include "hyperzoekt.surrealUrl" . | quote }}
            - name: SURREALDB_USERNAME
              value: {{ .Values.surrealdb.external.username | default "root" | quote }}
            - name: SURREALDB_PASSWORD
              value: {{ .Values.surrealdb.external.password | default "root" | quote }}
            - name: SURREAL_NS
              value: {{ .Values.surrealdb.external.namespace | default "zoekt" | quote }}
            - name: SURREAL_DB
              value: {{ .Values.surrealdb.external.database | default "repos" | quote }}
            {{- with .Values.hyperzoektHiragWorker.env }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          resources:
            {{- toYaml .Values.hyperzoektHiragWorker.resources | nindent 12 }}
      {{- with .Values.hyperzoektHiragWorker.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.hyperzoektHiragWorker.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.hyperzoektHiragWorker.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}

---
{{- if and (hasKey .Values "hyperzoektHiragWorker") (and .Values.hyperzoektHiragWorker.enabled (eq .Values.hyperzoektHiragWorker.exposure.type "service")) }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "hyperzoekt.fullname" . }}-hyperzoekt-hirag-worker
  labels:
    {{- include "hyperzoekt.labels" . | nindent 4 }}
    app.kubernetes.io/component: hyperzoekt-hirag-worker
spec:
  type: {{ .Values.hyperzoektHiragWorker.exposure.serviceType }}
  ports:
    - port: {{ .Values.hyperzoektHiragWorker.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hyperzoekt.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: hyperzoekt-hirag-worker
{{- end }}
