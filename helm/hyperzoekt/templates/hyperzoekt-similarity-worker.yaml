{{- if .Values.hyperzoektSimilarityWorker.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "hyperzoekt.fullname" . }}-hyperzoekt-similarity-worker
  labels:
    {{- include "hyperzoekt.labels" . | nindent 4 }}
    app.kubernetes.io/component: hyperzoekt-similarity-worker
  {{- with .Values.hyperzoektSimilarityWorker.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.hyperzoektSimilarityWorker.replicaCount }}
  selector:
    matchLabels:
      {{- include "hyperzoekt.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: hyperzoekt-similarity-worker
  template:
    metadata:
      labels:
        {{- include "hyperzoekt.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: hyperzoekt-similarity-worker
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "hyperzoekt.serviceAccountName" . }}
      containers:
        - name: hyperzoekt-similarity-worker
          image: {{ .Values.hyperzoektSimilarityWorker.image.registry }}/{{ .Values.hyperzoektSimilarityWorker.image.repository }}:{{ .Values.hyperzoektSimilarityWorker.image.tag }}
          imagePullPolicy: {{ .Values.hyperzoektSimilarityWorker.image.pullPolicy }}
          command: ["/app/hyperzoekt-similarity-worker", "--host", "0.0.0.0", "--port", {{ .Values.hyperzoektSimilarityWorker.service.port | quote }}]
          ports:
            - name: http
              containerPort: {{ .Values.hyperzoektSimilarityWorker.service.port }}
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          env:
            - name: REDIS_URL
              value: {{ include "hyperzoekt.redisUrl" . | quote }}
            - name: SURREALDB_URL
              value: {{ include "hyperzoekt.surrealUrl" . | quote }}
            - name: SURREALDB_USERNAME
              value: {{ .Values.surrealdb.external.username | default "root" | quote }}
            - name: SURREALDB_PASSWORD
              value: {{ .Values.surrealdb.external.password | default "root" | quote }}
            - name: SURREAL_NS
              value: {{ .Values.surrealdb.external.namespace | default "zoekt" | quote }}
            - name: SURREAL_DB
              value: {{ .Values.surrealdb.external.database | default "repos" | quote }}
            - name: HZ_LLM_URL
              value: {{ .Values.defaultLlmUrl | quote }}
            {{- with .Values.hyperzoektSimilarityWorker.env }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          resources:
            {{- toYaml .Values.hyperzoektSimilarityWorker.resources | nindent 12 }}
      {{- with .Values.hyperzoektSimilarityWorker.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.hyperzoektSimilarityWorker.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.hyperzoektSimilarityWorker.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}

---
{{- if and .Values.hyperzoektSimilarityWorker.enabled (eq .Values.hyperzoektSimilarityWorker.exposure.type "service") }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "hyperzoekt.fullname" . }}-hyperzoekt-similarity-worker
  labels:
    {{- include "hyperzoekt.labels" . | nindent 4 }}
    app.kubernetes.io/component: hyperzoekt-similarity-worker
spec:
  type: {{ .Values.hyperzoektSimilarityWorker.exposure.serviceType }}
  ports:
    - port: {{ .Values.hyperzoektSimilarityWorker.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hyperzoekt.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: hyperzoekt-similarity-worker
{{- end }}

