services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 3

  surrealdb:
    image: surrealdb/surrealdb:latest
    ports:
      - "8000:8000"
    command: start --user root --pass root memory
    restart: unless-stopped
  # Note: no in-container healthcheck because the surrealdb image may not include curl/wget.
  # An external prober service below performs the HTTP health check instead.

  zoekt-indexer:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "7676:7676"
    environment:
      - DISTRIBUTED_ZOEKT_REPO_EVENTS=1
      - REDIS_URL=redis://redis:6379
      - RUST_LOG=debug      
      - SURREALDB_URL=surrealdb:8000
      - SURREALDB_USERNAME=root
      - SURREALDB_PASSWORD=root
      - SURREAL_NS=zoekt
      - SURREAL_DB=repos
      - ZOEKTD_BIND_ADDR=0.0.0.0:7676
      - ZOEKT_ENDPOINT=http://zoekt-indexer:7676
    command: ["/app/dzr-indexer", "--host", "0.0.0.0", "--port", "7676"]
    # restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
      surrealdb-prober:
        condition: service_healthy

  zoekt-admin:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "7878:7878"
    environment:
      - REDIS_URL=redis://redis:6379
      - RUST_LOG=debug
      - SURREALDB_URL=surrealdb:8000
      - SURREALDB_USERNAME=root
      - SURREALDB_PASSWORD=root
      - SURREAL_NS=zoekt
      - SURREAL_DB=repos
      - ZOEKT_ADMIN_USERNAME=admin
      - ZOEKT_ADMIN_PASSWORD=password
    command: ["/app/dzr-admin", "--host", "0.0.0.0", "--port", "7878"]
    depends_on:
      redis:
        condition: service_healthy
      surrealdb-prober:
        condition: service_healthy

  zoekt-search:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - REDIS_URL=redis://redis:6379
      - RUST_LOG=debug
      - SURREALDB_URL=surrealdb:8000
      - SURREALDB_USERNAME=root
      - SURREALDB_PASSWORD=root
      - SURREAL_NS=zoekt
      - SURREAL_DB=repos
      - ZOEKTD_BIND_ADDR=0.0.0.0:8080
    command: ["/app/dzr-http-search", "--host", "0.0.0.0", "--port", "8080"]
    depends_on:
      redis:
        condition: service_healthy
      surrealdb-prober:
        condition: service_healthy

  zoekt-mcp:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "7979:7979"
    environment:
      - REDIS_URL=redis://redis:6379
      - RUST_LOG=debug
      - SURREALDB_URL=surrealdb:8000
      - SURREALDB_USERNAME=root
      - SURREALDB_PASSWORD=root
      - SURREAL_NS=zoekt
      - SURREAL_DB=repos
      - ZOEKTD_BIND_ADDR=0.0.0.0:7979
    command: ["/app/dzr-mcp-search", "--host", "0.0.0.0", "--port", "7979"]
    depends_on:
      redis:
        condition: service_healthy
      surrealdb-prober:
        condition: service_healthy

  hyperzoekt-indexer:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "7677:7677"
    environment:
      - REDIS_URL=redis://redis:6379
      - RUST_LOG=debug
      - SURREALDB_URL=http://surrealdb:8000
      - SURREALDB_USERNAME=root
      - SURREALDB_PASSWORD=root
      - SURREAL_NS=zoekt
      - SURREAL_DB=repos
    command: ["/app/hyperzoekt-indexer", "--host", "0.0.0.0", "--port", "7677"]
    # restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
      surrealdb-prober:
        condition: service_healthy

  hyperzoekt-webui:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8081:8081"
    environment:
      - REDIS_URL=redis://redis:6379
      - RUST_LOG=debug
      - SURREALDB_URL=http://surrealdb:8000
      - SURREALDB_USERNAME=root
      - SURREALDB_PASSWORD=root
      - SURREAL_NS=zoekt
      - SURREAL_DB=repos
    command: ["/app/hyperzoekt-webui", "--host", "0.0.0.0", "--port", "8081"]
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
      surrealdb-prober:
        condition: service_healthy

  surrealdb-prober:
    image: curlimages/curl:8.3.0
    command: ["tail", "-f", "/dev/null"]
    depends_on:
      surrealdb:
        condition: service_started
    healthcheck:
      test: ["CMD-SHELL", "curl -sS http://surrealdb:8000/health -o /dev/null -w '%{http_code}' | grep -q '^200$'"]
      interval: 10s
      timeout: 3s
      retries: 6
      start_period: 5s
