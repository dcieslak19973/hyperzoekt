# Port variables for docker-compose
REDIS_PORT=6379
SURREALDB_PORT=8001
ZOEK_INDEXER_PORT=7200
ZOEK_ADMIN_PORT=7201
ZOEK_ADMIN_POLLER_PORT=7202
ZOEK_SEARCH_PORT=7203
ZOEK_MCP_PORT=7204
HYPERZOEKT_INDEXER_PORT=7205
HYPERZOEKT_WEBUI_PORT=7206
HYPERZOEKT_MCP_PORT=7207
HYPERZOEKT_SIMILARITY_WORKER_PORT=7208
HYPERZOEKT_SBOM_WORKER_PORT=7209
TEI_HTTP_PORT=8088
HYPERZOEKT_LOG_LEVEL='hyperzoekt=debug,html5ever=warn'

# OpenAI API keys - fill these in locally. DO NOT commit your API keys to git.
# Precedence: the project-specific `HZ_OPENAI_API_KEY` is checked first and
# overrides `OPENAI_API_KEY` if both are present. Set either; prefer `HZ_` for
# repo-local configuration.
OPENAI_API_KEY=
HZ_OPENAI_API_KEY=

HZ_EMBED_TOKEN_LIMIT=4096
HZ_EMBED_MAX_BYTES=10000
# Path inside container where embedding DLQ records are written. Mount the
# host folder into the container (example: ./docker/embed-dlq:/tmp) so the
# DLQ is visible on the host at ./docker/embed-dlq/hz_embed_dlq.log
# Override at runtime if you prefer a different location.
HZ_EMBED_DLQ_PATH=/tmp/hz_embed_dlq.log